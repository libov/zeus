#include<TDataset.h>
#include<TSubSet.h>
#include<TCrossSection.h>

#include<iostream>

using namespace std;

int main(){

/*
	// example on how to work with dataset base
        TDataset myDataset("samples.v04.test.xml");
        // get a TSubSet object with these properties
	TSubSet cSubSet = myDataset.getSubSet(TSubSet::kMC, TSubSet::k05E, TSubSet::kLIGHT, TSubSet::kQ2g4);
        cSubSet.setLuminosityRecalculated(10000.);
        myDataset.modifySubSet(TSubSet::kMC, TSubSet::k05E, TSubSet::kLIGHT, TSubSet::kQ2g4, cSubSet);

        // that modified of a DOM object in memory
        // now write to disk actually
        myDataset.WriteXMLfile();
*/


        

	return 0;
}
